## Copy This Exact vercel.json Content:

```bash
cd ~/Desktop/projects/lush-properties-platform

# Replace vercel.json with this exact content:
cat > vercel.json << 'EOF'
{
  "version": 2,
  "functions": {
    "api/index.js": {
      "runtime": "nodejs18.x"
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/public"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
EOF

# Now commit and push:
git add .
git commit -m "Fixed SPA routing with rewrites configuration"
git push origin main
```

This replaces the complex routing with simple rewrites that properly handle React Router SPA fallback.